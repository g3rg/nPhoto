[x] handle 'X' quitting on main window
[x] remember window width / height!
[x] create settings dialog shell and show
[x] add settings
	[x] import dir
	[x] list of backup dirs
	[x] library directory
[x] Window title

[ ] Do Import!
        [x] make sure we have a library folder
        [x] make sure we have an import folder
        [x] check for duplicates?
        [x] copy all non duplicates
        [x] verify they have been copied
        [x] prompt for comment and keywords on import
		[x] create sidecar file with comment and keywords

[ ] Do Backup!
        [x] make sure we have a library folder
        [x] make sure we have at least one backup folder
        [x] make sure backup folders are valid
        [x] check if bkup folders are empty?
        [x] copy library folder to bkup folders!

[ ] Review 
	[ ] Load photos
		[ ] albums 
		[ ] photos and albums in albums!
		
	[ ] View individual photo
		[ ] autorotate based on EXIF data,
		[ ] next / prev image
		[ ] delete
		[ ] edit keywords - sidecar
		[ ] edit comment - sidecar

	[ ] Browser
		[ ] Thumbnails - 3 x 3? configurable?
		[ ] autorotate based on EXIF data
		[ ] next / prev page buttons
		[ ] multi-select
		[ ] delete selected		
	
	[ ] display EXIF
	[ ] searching! - build XML library? sqllite?

[ ] read EXIF on import
[ ] IPTC keywords
[ ] JPEG comments
[ ] RAW sidecar files?
